# Dependencies for Tipsy Elite - Machine à Cocktails
# Python 3.8+ required

# Core dependencies
pygame>=2.5.0                # Interface graphique et gestion tactile
RPi.GPIO>=0.7.1             # Contrôle GPIO Raspberry Pi (uniquement sur Pi)
asyncio-mqtt>=0.13.0        # Communication MQTT asynchrone (optionnel)

# Data handling
pydantic>=2.4.0             # Validation de données structurées
python-dotenv>=1.0.0        # Variables d'environnement
pathlib-ng>=1.0.0           # Gestion avancée des chemins

# Hardware and sensors
adafruit-circuitpython-motor>=3.4.0    # Contrôle moteurs avancé (optionnel)
w1thermsensor>=2.0.0        # Capteurs de température (optionnel)

# Development and testing
pytest>=7.4.0               # Tests unitaires
pytest-asyncio>=0.21.0      # Tests asynchrones
black>=23.0.0               # Formatage de code
flake8>=6.0.0              # Vérification syntaxe
mypy>=1.5.0                # Vérification types

# Optional dependencies for extended features
numpy>=1.24.0              # Calculs numériques (pour calibrage pompes)
opencv-python>=4.8.0       # Vision par ordinateur (reconnaissance verres)
pillow>=10.0.0             # Traitement d'images
requests>=2.31.0           # Communication HTTP
fastapi>=0.103.0           # API REST (pour contrôle distant)
uvicorn>=0.23.0            # Serveur ASGI

# Logging and monitoring
structlog>=23.1.0          # Logging structuré
prometheus-client>=0.17.0   # Métriques Prometheus (optionnel)

# Audio (pour notifications sonores)
pygame-mixer>=2.5.0        # Effets sonores (inclus dans pygame)
pyttsx3>=2.90              # Synthèse vocale (optionnel)

# Documentation
sphinx>=7.1.0              # Génération documentation
sphinx-rtd-theme>=1.3.0    # Thème documentation